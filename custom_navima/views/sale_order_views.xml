<?xml version='1.0' encoding='UTF-8'?>
<odoo>
    <record id="custom_navima_sale_order_form" model="ir.ui.view">
        <field name="name">custom.navima.view.sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="team_id" position="after">
                <field name="pnt_gestor_interno_id"/>
            </field>
        </field>
    </record>

    <record id="custom_navima_sale_order_tree" model="ir.ui.view">
        <field name="name">custom.navima.view.sale.order.tree</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree"/>
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="pnt_gestor_interno_id" optional="show"/>
            </field>
        </field>
    </record>
</odoo>
